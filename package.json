{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "read-shunt-plugin",
  "version": "0.3.0",
  "description": "Reduce large read results before they enter OpenCode, Codex, or Pi context.",
  "type": "module",
  "license": "MIT",
  "author": "p5",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/p5/read-shunt-plugin.git"
  },
  "homepage": "https://github.com/p5/read-shunt-plugin#readme",
  "bugs": "https://github.com/p5/read-shunt-plugin/issues",
  "keywords": [
    "opencode",
    "opencode-plugin",
    "codex",
    "codex-plugin",
    "pi",
    "pi-package",
    "token-optimization"
  ],
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "extensions",
    "hooks",
    ".codex-plugin",
    ".agents",
    "benchmarks",
    "server.ts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "check:build": "bun scripts/build.ts && tsc -p tsconfig.build.json",
    "check:package": "publint",
    "eval:live": "bun run eval/live.ts",
    "lint": "oxlint src test scripts eval",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opencode-ai/plugin": "beta"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.80.7",
    "@earendil-works/pi-coding-agent": "^0.80.7",
    "@types/bun": "latest",
    "@types/json-schema": "latest",
    "oxlint": "^1.15.0",
    "publint": "^0.3.24",
    "typescript": "latest"
  },
  "engines": {
    "bun": ">=1.2"
  },
  "pi": {
    "extensions": [
      "./extensions/read-shunt.ts"
    ]
  }
}
